# HANDOFF.md — 最初に読むファイル

## 0. あなた(AI)への依頼

このプロジェクトは Roblox の破壊ゲームを個人開発しているものです。
これまで別のAIアシスタントと進めてきた設計・実装の経緯が、同じフォルダの
`CURRENT_SPEC.md` / `THREAT_DESIGN_PROPOSAL.md` / `SETUP.md` / `Config.lua` に蓄積されています。

**このファイルは「仕様」ではなく「進め方のルール」です。**
仕様の中身は `CURRENT_SPEC.md` が唯一の正です。食い違いがあれば必ず `CURRENT_SPEC.md` を優先してください。

回答するときは、以下を守ってください。

- **できるだけかみ砕いた言葉で説明する**(専門用語を出すときは一言添える)
- **私が手を動かす必要がある作業は、具体的な手順まで書く**(Windows 環境。Roblox Studio の
  どのメニューをクリックするか、まで書いてほしい)
- 数値を提案するときは、**その数字にした理由**を必ず添える。理由のない数値は、後から見た人に
  「適当に決めた値」と誤解されて根拠なく変更される

---

## 1. どんなゲームか

- **ジャンル**: 街を武器で破壊するアクション。Roblox 向け(PC + モバイル/タブレット両対応)
- **1ラウンドの流れ**: `LOBBY`(街を生成)→ `BATTLE`(制限時間内に破壊)→ `RESULT`(結果発表)の無限ループ
- **街**: 4×4 の街区を格子状に並べたプロシージャル生成(グリッドモード)。約 13,500〜15,500 パーツ
- **武器3種**: バズーカ / エアストライク / リモート爆弾
- **スコア**: ブロック破壊 +10点、建物の全壊ボーナス +500点(1棟につき1回だけ)
- **敵システム**: スコアが閾値を超えると★(危険度)が上がり、警察→軍隊と敵が強くなっていく。
  敵の攻撃を受けると**残り時間が減る**(HPではなく時間が体力に相当する)
- **目標**: 給与以外の収入源にすること。Roblox で広く遊ばれる作品にしたい

---

## 2. いま何合目か(2026年8月時点)

敵システムは `THREAT_DESIGN_PROPOSAL.md` §6 の Step 0〜7 の順で作っています。

| Step | 内容 | 状態 |
|---|---|---|
| Step 0 | 基盤整備(RoundClock / ThreatManager / EnemyManager の3モジュール新設) | ✅ 完了 |
| Step 1 | タイム経済の可視化 | ✅ 完了 |
| Step 2 | ★1(警官のみ) | ✅ 完了 |
| Step 3 | ★1完成(パトカー + 道路走行AI + 警官輸送) | ✅ 完了 |
| **Step 4** | **武器改修(4a リモート爆弾の距離制限 / 4b 連鎖ボーナス / 4c エアストライクの絨毯爆撃化)** | **← 次はここ** |
| Step 5 | ★2(ヘリ + 増援) | 未着手 |
| Step 6 | ★3(戦車 + 建物破壊) | 未着手 |
| Step 7 | ★4 の拡張口の確認 | 未着手 |

### Step 4 を飛ばしてはいけない理由

`CURRENT_SPEC.md` §10 に書いてありますが重要なので再掲します。

**Step 3 時点のバランス測定値はすべて無効です。** 連鎖ボーナスと絨毯爆撃はスコアの伸び方を
根本から変えるため、Step 4 の前に★の閾値やラウンド時間を調整しても無駄になります。

また Step 4 は、いま一番大きな問題である「**プレイヤーが移動しない**」(`CURRENT_SPEC.md` §11-1)
への対策も兼ねています。バズーカに射程制限が無く、パトカーが至近まで来るため、
立ったまま1ラウンド終えられてしまう状態です。

---

## 3. 作業の進め方(この流れを崩さない)

```
① 設計・方針の相談          → チャットで議論する(あなたの出番)
② 方針が確定したら          → あなたが「指示書(markdown)」を出力する
③ 指示書を Claude Code に渡す → 実際のコード変更はそこで行う
④ Roblox Studio でプレイテスト → 結果をチャットに持ち帰る
⑤ CURRENT_SPEC.md を更新     → 次の議論の土台にする
```

**軽い相談はチャット内で完結させてOK**です。スニペットを1個見せれば済む話に指示書は要りません。
ただし **ファイルをまたぐ編集や、既存スクリプトへの機能追加は必ず指示書 → Claude Code** です。

### 指示書のフォーマット

これまで使ってきた型です。踏襲してください。

1. **番号付きセクション**(1. 目的 / 2. 変更するファイル / 3. 実装内容 …)
2. **Config 追加値の表**(キー / 値 / 意味 / なぜその値か)
3. **受け入れ基準のチェックリスト**(`- [ ]` 形式。Studio で何を見れば完了と言えるか)
4. **SETUP.md の更新指示**(手順書に追記が必要な場合)
5. **既存の動作を壊さないための注意点**

### Claude Code に渡すプロンプトの決まりごと

- **必ず「実装する前に、変更計画を報告してください」の一文を入れる。**
  いきなり書き換えられると、意図と違う実装が広範囲に入って戻すのが大変になります
- 一度に渡す指示書は **1つだけ**。複数を同時に投げると変更が衝突します
- **バグ修正が機能追加より先**

### モデルの使い分け(Claude Code 側)

| モデル | 用途 |
|---|---|
| **Opus** | アーキテクチャ設計、モジュール構成の判断、設計レベルの相談 |
| **Sonnet** | 実装、ドキュメント更新。**通常はこれ** |
| **Haiku** | 使わない。特にファイル横断の作業では絶対に使わない |

---

## 4. 設計上の原則

- **Config 駆動にする。** 数値をコードに直書きしない。`ReplicatedStorage/Config.lua` に出して、
  コードを触らずに調整できる形にする
- **一度に1仕様ずつ。** まとめて入れると、どの変更が悪さをしたか分からなくなる
- **各変更のあと、必ず Studio のプレイテストで確認してから次に進む**
- **パフォーマンスは実測で決める。** 目標は「対象タブレット実機で 30fps 維持」。
  仕様書の理論値ではなく実機の数字で判断する
- **プレイテストに委ねてよい判断と、コードを書く前に決めなければいけない判断を分ける。**
  「この数値がちょうどいいか」は実機で試せばいい。「そもそもこの仕組みで成立するか」は先に詰める

---

## 5. Roblox 固有のハマりどころ(実際に踏んだもの)

再発防止のため、必ず読んでから技術的な提案をしてください。

| 事象 | 結論 |
|---|---|
| **パフォーマンスのボトルネック** | 総パーツ数ではなく、**爆発時に物理演算される(Anchored=false の)パーツ数**。静的なパーツは何万個あっても軽い。効くのは `MaxUnanchoredParts` の制御 |
| **`TextChatService:DisplayBubble()`** | **サーバーから動かない。** 2026年初頭から広く壊れている。フキダシは `BillboardGui` で自作すること |
| **Terrain の草が道路を隠す** | 道路の高さを上げるのではなく、`Terrain:FillBlock` に `Air` マテリアルを指定して道路の下を掘る |
| **Toolbox の街モデル** | 破壊対象の建物には**使えない**(構造がバラバラでブロック分解できない)。破壊しない小物なら可 |
| **NPC に Humanoid を使わない** | Humanoid / Motor6D は重い。Part の組み立て + `WeldConstraint` で作る(既存の `NPCManager.lua` 参照) |
| **Roblox の推薦アルゴリズム** | セッションの長さより**再訪(28日窓・1ユーザーあたり1日60分が上限)**を重視する。目標は「明日また来る」であって「長く遊ばせる」ではない |
| **リーチ解放の条件** | 60日以内に「16歳以上の熱心なプレイ 500回」が必要。待っていても増えないので、能動的な集客が要る |

---

## 6. 決着済みの論点(蒸し返さないでください)

議論のやり直しを防ぐためのリストです。変更を提案する場合は、**下の「理由」に反論する形で**
提案してください。

| 決定 | 理由 |
|---|---|
| **敵はタイムを配らない**(警官の `TimeReward = 0`) | スコアは「進行度」、タイムは「生存」で意味が違う。敵がタイムを配ると、敵が脅威ではなく「生存資源の配達人」になる。実機で「敵を待っていれば時間が増える」状態になったため外した。スコア(300点)は残す ── スコアが増えると★が上がって危険も増すので、「危険と引き換えの取引」として正しく機能する |
| **アイテムドロップで「時間」を落とさない** | 上と同じ理由。供給源が敵から瓦礫に移るだけになる。有力案は**武器の出力**(絨毯爆撃1回分など) |
| **パトカーの降車回数は無制限**(`MaxDeployTrips = nil`) | 有限にすると「使い切ったパトカーを1台わざと残せば次の波が永久に来ない」抜け道ができる。無制限なら放置コストが無限になり抜け道が塞がる。**有限に戻すなら、離脱して消滅する等の対策を同時に入れること** |
| **敵の HP を上げてバランスを取らない** | パトカーの `Hits` を 5 にしたら「敵が強い」ではなく「クリックが疲れる」という結果になり 3 に戻した。**敵の HP を上げることは、プレイヤーの腕力を通貨にして難易度を買うことに等しい** |
| **レーダー(ミニマップ)は作らない** | 頭上マーカー + 画面端の方向インジケータの2本立てで代替する |
| **グリッドモードが本番** | 従来モード(十字路1本 + 建物13棟)は凍結。コードは消さず温存してあり、`CityGenerator.lua` 冒頭の `USE_GRID_MODE` を `false` にすれば戻る |
| **ラウンド時間は残り時間の増減方式** | `RoundClock` が deadline 方式で管理。損失には「直近60秒で最大20秒まで」のローリング上限がある(これが無いと★1で毎秒1.33秒減る計算になり破綻する) |

---

## 7. ファイルの役割

| ファイル | 役割 | 更新するのは誰か |
|---|---|---|
| `CURRENT_SPEC.md` | **唯一の正**。「今こうなっている」の現状スナップショット。Config 全キー・生成式・破壊処理・NPC・決定事項・未解決課題 | 実装が終わるたびに Claude Code(Sonnet)で更新 |
| `THREAT_DESIGN_PROPOSAL.md` | 敵システムの設計提案。Step 0〜7 の実装順序と、その理由 | 設計変更時のみ |
| `SETUP.md` | Roblox Studio への配置手順・動作確認チェックリスト | 新モジュール追加時 |
| `Config.lua` | 全設定値の実体 | 実装のたび |
| `PROGRESS.md` | 実装履歴・暫定措置・ハマった点の記録 | 各ステップ完了時 |
| `HANDOFF.md`(この文書) | 進め方のルール | ルールが変わったとき |

`archive/` 内の古い指示書は過去の変更指示であり、**`CURRENT_SPEC.md` が上書きします。**

---

## 8. 環境

- **OS**: Windows
- **エディタ**: VS Code。ファイルは Roblox のサービス名でフォルダ分けしてある
- **同期**: Rojo(**ポート 34873**。34872 は別プロジェクトで使用中)。
  日本語 + OneDrive のパスに対応するため、`%~dp0` でパスを解決する `.bat` から起動している
- **実機テスト**: タブレット(30fps 維持が合格ライン)

---

## 9. やってはいけないこと

- **`CURRENT_SPEC.md` を読まずに仕様を推測して答える**(§6 の決着済み論点を蒸し返す原因になる)
- **複数の変更をまとめて1つの指示書に入れる**
- **未実装の敵種別(`Helicopter` / `Tank`)を `Config.Threat.Stages` に登録する** → エラーで落ちる
- **Step 4 より先に★2/★3の閾値やラウンド時間を調整する** → 測り直しになる
- **数値だけ変えて理由を書かない**

---

## 10. 会話を終えるとき

長い会話の最後には、**次の会話に渡す申し送りメモ**を作ってください。含めるもの:

- この会話で決まったこと(と、その理由)
- 保留にしたこと
- 次にやること
- `CURRENT_SPEC.md` に反映が必要な箇所

トークン消費を抑えるため、大きな区切りごとに新しい会話を始めています。
