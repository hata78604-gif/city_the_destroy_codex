# 実装指示書: 街並みリアル化 Step V-2(屋根の向き・瓦礫の当たり判定・小物の破壊)

作成日: 2026-08-07
前提文書: `CURRENT_SPEC.md` / `VISUAL_DESIGN_PROPOSAL.md` / `SPEC_VISUAL_STEP1.md`
推奨モデル: **Sonnet**(3件とも局所的な修正。Opusは不要)

---

## 0. この指示書の位置づけ

Step V-1(パレット・三角屋根・石垣・街小物)の実装後、実機プレイテストで見つかった
3件の修正を行う。**新機能の追加ではなく、V-1の仕上げである。**

| # | 内容 | 種別 |
|---|---|---|
| 1 | 切妻屋根の尾根の向きが90度ずれている | **バグ修正** |
| 2 | 瓦礫が道を塞いで通行できない | 挙動の改善 |
| 3 | 街小物(街灯・車・木・ベンチ)を破壊可能にする | 仕様変更 |

3件は互いに独立している。実装順序は問わない。

### 変更対象と規模の見積もり

| ファイル | 変更内容 | 概算行数 |
|---|---|---|
| `ServerScriptService/Modules/CityGenerator.lua` | 屋根の向き(§1)、小物への`Destructible`付与(§3) | 約 40行 |
| `ServerScriptService/Modules/DestructionManager.lua` | 瓦礫の当たり判定の解除(§2) | 約 40行 |
| `ReplicatedStorage/Config.lua` | `Config.Debris` にキー追加(§2) | 約 10行 |
| `CURRENT_SPEC.md` | 仕様と設計判断の追記 | 約 40行 |
| `SETUP.md` | 確認項目と調整値の説明を更新 | 約 20行 |
| `PROGRESS.md` | 実装記録の追記 | 適量 |

**コード3ファイル / ドキュメント3ファイル。合計 約150行。**

---

## 1. 着手前に必ず行うこと(計画報告)

**コードを1行も変更する前に、以下を報告して承認を待つこと。**

1. 変更するファイルと変更箇所(関数名レベル)
2. §1について、現在の屋根生成コードで**尾根の向きが何を基準に決まっているか**を読んで報告する
   (`sizeX`/`sizeZ` の長辺で決めているのか、固定なのか、`rotationY` を見ているのか)
3. §2について、`DestructionManager` の瓦礫キュー(FIFO・寿命フェード)の現在の構造を踏まえ、
   提案する実装方法が既存の仕組みと衝突しないか
4. この指示書の記述であいまい・矛盾していると感じた箇所

---

## 2. §1: 切妻屋根の尾根の向きを直す(バグ修正)

### 2-1. 現状

実機で確認したところ、**尾根(屋根のてっぺんの稜線)が道路と垂直**に走っている。
形状としては切妻屋根で正しく、てっぺんも稜線になっている(点や平面にはなっていない)。
**問題は向きだけである。**

`Config.Visual.Roof.GableHeight` を 6 → 12 に変えると、意図しない方向の傾斜が急になった。
これも「向きが90度ずれている」ことの裏付けである。

### 2-2. あるべき姿

**尾根は、その建物の正面(道路に面した側)と平行に走らせる。**

現実の街並みでは、道路から見えるのは「長い屋根の斜面」であり、三角形の妻面ではない
(平入り)。参考にした街並みも全てこの形だった。**この向きの統一が街並みの整った印象を作る。**

### 2-3. 基準にするもの

**建物の長辺(`sizeX`/`sizeZ` の大小)で決めてはいけない。**
テンプレートによって長辺が正面側だったり側面側だったりするため、街全体でバラつく。

**`generateGridSlots()` が各スロットに与えている `rotationY`(建物の正面の向き)を基準にする。**
グリッドモードでは建物の正面が必ず道路側を向いているため
(南辺=0°, 北辺=180°, 西辺=90°, 東辺=-90°)、この基準なら街全体で尾根の向きが揃う。

### 2-4. 分割方向も連動して変わる

Step V-1で「屋根は棟方向に `Config.Block.Size.X`(=8)単位で分割する」と指示した。
**尾根の向きが変わるので、分割する方向も同じだけ回る。**分割そのものは維持すること
(巨大な1枚パーツにしない、というルールは変わらない)。

### 2-5. 従来モードの扱い

従来モード(`USE_GRID_MODE=false`)には `rotationY` の概念が
グリッドモードと同じ形で存在するとは限らない。

- 従来モードでも同じ基準が使えるなら、そのまま適用してよい
- 使えない場合は、**従来モードは長辺基準のままで構わない**(凍結中のモードのため)
- どちらにしたかを計画報告で述べること

---

## 3. §2: 瓦礫が止まったら当たり判定を切る

### 3-1. 現状と原因

石垣・街小物が入って**通行できる場所が道路だけ**になった結果、
破壊で発生した瓦礫が道路上に堆積し、プレイヤーが物理的に通れなくなった。

暫定対処として `Config.Performance.DebrisLifetime` を 8 → 3 に短縮し、
`Config.City.RoadWidth` を 16 → 24 に広げて緩和済み。
**ただしこれは対症療法であり、瓦礫の寿命を縮めることで「壊した跡」の達成感を犠牲にしている。**

### 3-2. あるべき姿

**飛んでいる最中は当たり判定あり、落ちて止まったら当たり判定なし。**

| 状態 | `CanCollide` | 理由 |
|---|---|---|
| 爆発直後・飛行中 | `true` | 吹き飛ぶ迫力・他の瓦礫との衝突が破壊の手応えを作る |
| 落下して静止後 | **`false`** | 通行の妨げにしない |

見た目は残るため「壊した跡」は視覚的に保たれる。
**これにより `DebrisLifetime` を長く戻せるようになる。**

これは `VISUAL_DESIGN_PROPOSAL.md` §C-3(破壊された残骸を街に残す)への布石でもある。
残骸を残す設計は「残骸が通行の邪魔をしない」ことが前提になるため、
ここで当たり判定の扱いを確定させておく。

### 3-3. 実装方針

**時間経過で切る方式を採用する。**

瓦礫を生成して物理を与えた時点から `Config.Debris.CollideTime` 秒後に `CanCollide = false` にする。

**速度を監視して「止まったら切る」方式は採らないこと。** 理由:

- 全瓦礫の速度を毎フレーム監視すると、瓦礫が多いときにコストが跳ねる
  (このゲームは1回の爆発で最大500個の本物瓦礫が飛ぶ)
- 斜面で滑り続ける・他の瓦礫の上で微振動するなど、「止まった」の判定が安定しない

時間で切れば、既存の寿命管理(FIFOキュー・`fadeAndRemove`)と同じ考え方で扱える。

### 3-4. 実装上の注意

- **本物の瓦礫(`destroyBlockReal`)とダミー破片(`spawnDummyDebris`)の両方に適用する**
  - ダミー破片が元から `CanCollide = false` の場合は、本物のみでよい。
    現在の実装を確認して計画報告で述べること
- 既存の瓦礫キュー(FIFO・`evictOldest`・`fadeAndRemove`)の構造を壊さないこと
- `CollideTime` が `DebrisLifetime` より大きい場合、当たり判定を切る前に瓦礫が消えるだけなので
  エラーにはならない。**特別な検証やクランプは不要**(値の整合はConfigを振る人の責任)
- 瓦礫が `CanCollide = false` になった後も、`CollisionGroup = "Debris"` の設定は変更しない

### 3-5. 追加するConfig

`Config.Debris` に1キー追加する。

| キー | 既定値 | 意味 |
|---|---|---|
| `CollideTime` | `1.5` | 瓦礫が発生してから当たり判定を失うまでの秒数 |

**実機で振る値である。**小さくすると早く通れるようになるが、瓦礫同士がぶつかる迫力が減る。

### 3-6. あわせて戻す値

この実装が入ったら、暫定対処で下げた値を戻す。

| キー | 現在 | 変更後 | 理由 |
|---|---|---|---|
| `Config.Performance.DebrisLifetime` | `3` | **`8`** | 当たり判定を切ることで通行問題が解決するため、元の値に戻す |

**`Config.City.RoadWidth = 24` は戻さない。**道幅24は見た目の改善としても採用済み
(街として自然になった)であり、瓦礫対策とは独立した決定である。

---

## 4. §3: 街小物を破壊可能にする

### 4-1. 経緯と判断

Step V-1では「建物破壊率とスコアの意味を変えない」ため、街小物(街灯・車・木・ベンチ)を
**破壊不可**にした。しかし実機で確認したところ、**目の前の車が爆発で無傷なのは
破壊ゲームとして違和感が強い。**

破壊可能にすると「小物狩りが最適解になる」という懸念があったが、**成立しない**と判断した。
理由: **小物は街全体に分散して配置されており、移動時間のほうが高くつく。**
建物は1か所に固まっているため、同じ時間なら建物を壊すほうが必ず効率がよい。

したがって**小物専用のスコア(`Config.Score.Prop` 等)は作らない。**
既存の `Config.Score.Block`(10点)をそのまま適用する。
**新しいConfigキーは追加しないこと。**

> 設計原則との整合: 「読む場所のないキー・振っても意味の変わらないキーは作らない」
> (`CURRENT_SPEC.md` §12-1)。ここで別のスコアキーを作っても振る理由が無い。

### 4-2. 変更内容

`buildGridProps` で生成する街灯・駐車車両・木・ベンチの全パーツに:

- **`Destructible` タグを付ける**
- **`BuildingId` 属性は付けない**

### 4-3. `BuildingId` を付けない理由(重要)

`BuildingId` は以下の3つに使われている。**小物が混ざると全て壊れる。**

1. **全壊ボーナス**(棟ごとに1回・500点)
2. **リザルトの建物破壊率**
3. **★2スナイパーの屋上候補探索**(`EnemyManager.findRooftopCandidates`)

3が特に危険で、`BuildingId` を付けると**街灯の上にスナイパーが立つ**。

### 4-4. 石垣との整合

石垣は Step V-1 時点で既に「`Destructible` あり / `BuildingId` なし」になっている。
**小物も完全に同じ扱いにする。**これで「建物以外の壊せるもの」の扱いが統一される。

### 4-5. 従来モードの扱い

従来モードの `buildProps` は**変更しない**(凍結中のモードのため)。
グリッド用の `buildGridProps` のみを変更する。

### 4-6. 意図した結果

ラウンドが進むと街灯・木・車が街から消えていく。**これは意図どおりである。**
破壊ゲームとして、壊せないものが街に立っているほうが不自然という判断による。

---

## 5. やらないこと(この指示書の範囲外)

- 道路の白線・横断歩道(A-1)
- `MaterialVariant` による質感(A-6)
- Terrain背景・霧・海(B-1〜B-3)
- 電柱・電線(A-7)
- 手作りテンプレートのグリッドモード有効化(D-1)
- 破壊後の焼け焦げた残骸・火煙(C-3/C-5)
- 生成処理の分散(`task.wait()`)
- **プレイヤー移動速度と敵 `MoveSpeed` の調整**
  (移動の遅さは既知の課題だが、見た目が固まってから★閾値の再測定と同時に行う)
- **★閾値・タイム経済の再測定**

---

## 6. ドキュメントの更新

### `CURRENT_SPEC.md`

以下の**設計判断を理由つきで記録**する。数値だけを書かないこと。

- **屋根の尾根は `rotationY` 基準で決める**(長辺基準にすると街全体でバラつくため)
- **瓦礫の当たり判定は時間で切る**(速度監視を採らなかった理由: コストと判定の不安定さ)
- **街小物に専用スコアキーを作らなかった理由**(小物は分散配置されており、
  移動時間のせいで建物より効率が悪い。したがって小物狩りは成立しない)
- **街小物・石垣に `BuildingId` を付けない理由**(全壊ボーナス・建物破壊率・
  スナイパーの屋上候補の3つが壊れる)
- `Config.Debris.CollideTime` を Config一覧に追加
- `Config.City.RoadWidth` を 16 → 24 に変更したことと理由を反映
- `Config.Performance.DebrisLifetime` を 8 に戻したことを反映
- §7(未実装のまま保留)の記述で、街小物が破壊不可であるという前提の記述があれば修正する

### `SETUP.md`

- 「街並み拡張の確認ポイント(グリッドモード)」の
  **「道路・歩道は爆発しても壊れない」は維持**しつつ、
  **街灯・車・木・ベンチは壊せるようになった**ことを反映する
- 「見た目の調整」または性能調整の節に `Config.Debris.CollideTime` の説明を追加
  (小さくすると早く通れるが、瓦礫同士がぶつかる迫力が減る)

### `PROGRESS.md`

いつもどおり「実装内容 / 暫定措置・妥協点 / ハマった点と対処 / 申し送り / ユーザー手作業」を追記。

---

## 7. 受け入れ基準

実装完了の報告前に、`roblox-playtest` スキルで実際にゲームを起動して確認すること。

### 屋根

- [ ] **尾根が道路と平行**に走っている(道路から見て、三角形の妻面ではなく長い斜面が見える)
- [ ] 街全体で尾根の向きが揃っている(建物ごとにバラバラでない)
- [ ] `GableHeight` を大きくすると、**意図した方向の傾斜が急になる**
- [ ] 屋根が複数パーツに分割されたままである(1枚の巨大パーツになっていない)
- [ ] `GableEnabled = false` で従来の平らな屋根に戻る

### 瓦礫

- [ ] 爆発直後、瓦礫が他の物にぶつかりながら飛ぶ(当たり判定が生きている)
- [ ] **数秒後、瓦礫の山の上を歩いて通り抜けられる**
- [ ] `DebrisLifetime = 8` に戻しても道が塞がらない
- [ ] 瓦礫が寿命でフェードして消える挙動が従来どおり動く
- [ ] `MaxUnanchoredParts` 超過時の最古瓦礫の即時削除(`evictOldest`)が従来どおり動く
- [ ] `CollideTime` を 0.2 など極端に小さくしても、エラーにならない

### 小物

- [ ] 街灯・車・木・ベンチが**爆発で壊れる**
- [ ] 壊すと10点入る
- [ ] **道路・歩道は壊れない**(従来どおり)
- [ ] 全壊ボーナス(+500点)が建物1棟につき1回だけ入る(小物で増えていない)
- [ ] リザルトの建物破壊率が、小物を壊しても変化しない
- [ ] ★2のスナイパーが**建物の屋上にのみ出現**する(街灯や木の上に立たない)

### 全般

- [ ] 合計パーツ数が `Config.Performance.MaxTotalParts`(35000)を超えない
- [ ] ラウンド終了→再生成で古いマップが残らない
- [ ] Outputに新しいエラー・警告が出ない(3分間プレイ)

### ユーザー手作業として報告すること

- タブレット実機での確認項目
- 実機で振ってほしい値(`CollideTime` / `GableHeight` / `DebrisLifetime`)と振り方の目安

---

## 8. 出力前のセルフレビュー

1. 従来モード(`USE_GRID_MODE=false`)のコードを変更していないか
2. `EnemyManager` を変更していないか(この指示書では触らない)
3. **新しいConfigキーが `Config.Debris.CollideTime` の1つだけ**に収まっているか
   (`Config.Score.Prop` 等を作っていないか)
4. 小物・石垣に `BuildingId` を付けていないか
5. 屋根の分割が維持されているか
6. バランス値(スコア配点・★閾値・タイム)を変更していないか
7. `CURRENT_SPEC.md` に既にある内容を `PROGRESS.md` に重複して書いていないか
