# Step 4d: バズーカの射程制限・押しっぱなし連射・スコア内訳ログ

## 0. 前提

- 単一の正の仕様書は `CURRENT_SPEC.md`。未解決課題の背景は同 §11-1
- **Step 4a+4b(リモート爆弾)と 4c(絨毯爆撃)が完了している前提**。まだなら先にそちらを終わらせること
- 本書は Step 4 の3本のうちの3本目。これが終わったら**バランスの再測定フェーズ**に入る

## 1. 実装前に必ずやること

コードを1行も書く前に、以下を調べて**実装計画を報告**すること。承認を待ってから着手する。

1. `WeaponServer.lua` のバズーカ処理(旧L159付近)。特に**弾が `MaxDistance` に到達したときに今どうなっているか**(その場で爆発するのか、黙って消えるのか)
2. `WeaponClient.client.lua` の `Tool.Activated` による発射処理
3. `UIController.client.lua` のモバイル用「発射」ボタンと、`WeaponClientEvents` の `FireRequest` BindableEvent の使われ方
4. `UIController.client.lua` の武器スロットのクールダウン暗転処理
5. スコアを加算している関数(`WeaponServer` 内)と、その呼び出し箇所すべて(`DestructionManager` / `NPCManager` / `EnemyManager`)

報告には「どのファイルの何行目をどう変えるか」を含めること。

## 2. 目的

| # | 内容 | 狙い |
|---|---|---|
| A | 射程を 400 → 140 に制限 | **立ち位置に意味を持たせ、プレイヤーが移動する理由を作る**(`CURRENT_SPEC.md` §11-1 の最優先項目) |
| B | 押しっぱなしで連射 + クールダウン0.3秒 | **腕の疲れを取る**。速度は落とさずに連打をやめさせる |
| C | スコアの内訳ログ | 次のフェーズで★閾値を再設定するための**測定用**。ゲーム体験は変えない |

**A と B は必ず同じコミットに入れること。** A だけを入れると「同じクリック回数に歩く手間が上乗せされる」だけになり、体験が今より悪化する。

## 3. Config の改修(`ReplicatedStorage/Config.lua`)

`Config.Weapons.Bazooka`:

| キー | 新しい値 | 旧値 | 備考 |
|---|---|---|---|
| `MaxDistance` | `140` | `400` | 射程 |
| `Cooldown` | `0.3` | `0` | 連射間隔 |
| `AutoFire` | `true` | 新規 | 押しっぱなしで連射するか |
| `Speed` | `100` | 同 | **変更しない** |
| `Radius` | `12` | 同 | **変更しない** |

`AutoFire` は `Bazooka` にだけ付ける。`Airstrike` と `RemoteBomb` には付けない(=単発のまま)。実装は「`AutoFire` が真の武器だけ連射する」という汎用の分岐にすること。

### 射程140の根拠(変更しないこと)

- 警官の `AttackRange = 100` より短いと、一方的に撃たれる時間ができる
- 街のタイルは124stud刻み(道路中心線 -248 / -124 / 0 / 124 / 248)。1区画を道路際から片付けるのに必要な長さがおおよそ124〜140
- 140なら「1か所に立つ → 1区画を片付ける → 次の交差点へ移る」というリズムになり、街全体を回るのに6〜9か所の立ち位置が必要になる

## 4. A: 射程制限と射程端での爆発

### 4-1. 挙動

- 弾は従来どおりクリック方向へ直進する
- 何にも当たらないまま飛距離が `MaxDistance` に達したら、**その地点で通常どおり爆発させる**
- 拒否メッセージは出さない。**「弾が空中で爆発するのを見れば射程が分かる」ことを狙う**ため、テキストによる説明はしない
- 途中で建物・敵・地面に当たった場合の挙動は従来どおり(変更しない)

### 4-2. 実装上の注意

- すでに射程端で爆発する実装になっていれば、**Config の数値変更だけで完了**する。その場合は「Config 1行のみ変更」と報告すること
- 黙って消える実装だった場合のみ、爆発呼び出しを追加する
- 爆発は `Explode(ctx)` を `source = "Bazooka"` で呼ぶ。`scoreScale` は渡さない

## 5. B: 押しっぱなし連射

### 5-1. クライアント(`WeaponClient.client.lua`)

- `Tool.Activated` で連射を**開始**し、`Tool.Deactivated` で**停止**する
- 連射中は `Config.Weapons[weapon].Cooldown` の間隔で発射要求を送る
- **狙点は毎回 `Mouse.Hit.Position` を取り直す。** 押したまま照準を動かせば、爆発がなぞるように走ること(これが疲労軽減の要)
- 以下のときは必ず停止する:
  - 武器を切り替えたとき
  - ツールが `Unequipped` になったとき
  - `RoundState` が `BATTLE` 以外になったとき(LOBBY / RESULT 中に撃ち続けない)
  - プレイヤーのキャラクターが失われたとき

### 5-2. モバイル(`UIController.client.lua`)

- 右下の「発射」ボタンを**押しっぱなしに対応**させる
- ボタンを押した瞬間に連射開始、離した瞬間に停止
- **ボタンの外に指がスライドして離れた場合も必ず停止すること**(押しっぱなしのまま止まらなくなる事故を防ぐ)
- タッチとマウスの両方で同じ挙動になること(Studio のデバイスエミュレータで確認)

### 5-3. サーバー(`WeaponServer.lua`)

- **サーバー側でも `Cooldown` を必ず強制する。** クライアントの連射間隔を信用しない
- 間隔より早い発射要求は**黙って捨てる**(エラーも通知も出さない)

### 5-4. クールダウンUIの抑制

0.3秒のクールダウンをそのまま表示すると、武器スロットが毎秒3回チカチカして目障りになる。

- **`Cooldown` が 0.5 未満の武器については、`Cooldown` リモートを送らない**(サーバー側で判定)
- しきい値の 0.5 は `WeaponServer.lua` の先頭に定数として定義し、意図をコメントで書いておく
- エアストライク(20秒)・リモート爆弾(1秒)の暗転表示は従来どおり動くこと

## 6. C: スコア内訳ログ(測定用)

★の閾値を再設定するために、**スコアが「建物由来」なのか「敵由来」なのか**を分けて見られるようにする。ゲーム体験は一切変えない。

### 6-1. 実装

- スコア加算関数に、任意引数 `category`(文字列)を追加する。省略時は `"other"`
- 呼び出し側で以下を渡す:

| 加点 | category |
|---|---|
| ブロック破壊(10点) | `"block"` |
| 全壊ボーナス(500点) | `"buildingBonus"` |
| 市民NPC撃破(100点) | `"npc"` |
| 敵撃破(警官300点 / パトカー500点) | `"enemy"` |

- ラウンド中、プレイヤーごとに category 別の累計を持つ
- ラウンド終了時(RESULT に入るとき)にサーバーの出力ウィンドウへ1行出す:

```
[Score] プレイヤー名: 建物 1200 / 全壊 1500 / 市民 400 / 敵 2100 / 合計 5200
```

- 累計は次のラウンド開始時にリセットする
- **リザルト画面(UI)には出さない。** 出力ウィンドウのログだけでよい

### 6-2. なぜ必要か

現在、警官は `MaxDeployTrips = nil`(無制限)で湧き続け、1体+300点が入る(`CURRENT_SPEC.md` §10-1)。押しっぱなし連射を入れると警官を倒すコストがさらに下がるため、**★の上昇が「街の破壊」ではなく「警官狩り」で決まる状態が強まる**可能性が高い。合計スコアだけを見ていると、この逆転に気づけない。

## 7. 受け入れ基準

### 非破壊確認(先にこれを見ること)

- [ ] エアストライク(絨毯爆撃)が従来どおり撃てる
- [ ] リモート爆弾の設置・距離制限・連鎖ボーナスが従来どおり動く
- [ ] 市民NPC・敵(パトカー・警官)が従来どおり動く
- [ ] ラウンドが LOBBY → BATTLE → RESULT を1周する

### A: 射程

- [ ] 街の中央に立って**反対側の端の建物**をクリックすると、弾が飛んでいって**手前の空中で爆発**する(建物は壊れない)
- [ ] 140stud 以内の建物は従来どおり壊せる
- [ ] 街の端から端まで1か所で壊し切ることが**できなくなっている**
- [ ] 警官(100から撃ってくる)を、**撃たれる前に倒せる位置がある**
- [ ] 空中で爆発した弾が、たまたま近くにいたNPCや敵を巻き込んでも問題なく処理される

### B: 連射

- [ ] マウスを押しっぱなしにすると、約0.3秒間隔で撃ち続ける
- [ ] 押したまま照準を動かすと、爆発がなぞるように移動していく
- [ ] ボタンを離すと止まる
- [ ] 武器を2または3に切り替えると止まる
- [ ] ラウンドが終わって RESULT になると止まる(リザルト画面の裏で撃ち続けない)
- [ ] エアストライク・リモート爆弾は**押しっぱなしで連射されない**(従来どおり単発)
- [ ] **武器スロットが0.3秒ごとにチカチカ暗転しない**
- [ ] エアストライク(20秒)・リモート爆弾(1秒)の暗転は従来どおり出る
- [ ] モバイルエミュレータで「発射」ボタン長押しが効く。ボタン外に指を滑らせて離しても止まる
- [ ] 連射中に目に見えるカクつきが出ない(4c で入れた演出の合流処理が効いていること)

### C: ログ

- [ ] ラウンド終了時、サーバーの出力ウィンドウに `[Score]` の行が出る
- [ ] 各項目の合計が、リザルト画面のスコアと一致する
- [ ] 次のラウンドで数字がリセットされている
- [ ] リザルト画面の見た目は従来と変わっていない

### エラー

- [ ] サーバー・クライアント両方の出力ウィンドウに赤いエラーが出ない

## 8. `SETUP.md` の更新

「ゲームバランスの調整」の節に追記:

- `Config.Weapons.Bazooka.MaxDistance`(既定140): バズーカの射程。**警官の `AttackRange`(既定100)より短くすると一方的に撃たれるので、下げるときは対で見ること**。短くするほどプレイヤーが移動する
- `Config.Weapons.Bazooka.Cooldown`(既定0.3): 連射間隔。小さくすると速いが瓦礫が増えて重くなる
- `Config.Weapons.Bazooka.AutoFire`(既定true): falseにすると1クリック1発に戻る

「遊び方(操作方法)」の表を更新:

| 操作 | PC | タブレット/スマホ |
|---|---|---|
| 発射(バズーカ) | クリック。**押しっぱなしで連射** | タップ、または「発射」ボタン**長押しで連射** |

「Phase 4」のチェック項目に、押しっぱなし連射の行を追加する。

## 9. やらないこと

- `Speed`(100)・`Radius`(12)の変更
- 警官の `AttackRange` / `StopDistance`、パトカーの `StopDistance` の変更(**Config の値なので、実装後に手動で試す**)
- ★閾値(`Config.Threat.Stages[].Threshold`)の変更(測定後に別途決める)
- リザルト画面へのスコア内訳の表示
- アイテムドロップに関する実装・設計

## 10. 完了後にユーザーが行う測定(Claude Code の作業ではない)

3本すべてが終わったら、以下を測る。結果を持って★閾値の再設定を行う。

1. Studio でプレイテストを開始し、**ラウンドを3回**通す
2. 各ラウンドについて記録する:
   - 出力ウィンドウの `[ThreatManager] ★1 警察 に到達 (開始から ○○ 秒 / スコア 1000)` の**秒数**
   - 出力ウィンドウの `[Score]` 行の**内訳**(建物 / 全壊 / 市民 / 敵)
   - ラウンドが終わった**時刻**(タイム増減があるので120秒とは限らない)
3. 体感として記録する:
   - 1ラウンド中に**何回くらい立ち位置を変えたか**
   - 腕が疲れたかどうか
4. **タブレット実機**でも1ラウンド通し、絨毯爆撃時に30fpsを割らないことを確認する
