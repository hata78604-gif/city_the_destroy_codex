# Step 4a + 4b: リモート爆弾の距離制限と連鎖ボーナス

## 0. 前提

- 単一の正の仕様書は `CURRENT_SPEC.md`。既存の値・構成はそちらを参照すること
- 設計の背景は `THREAT_DESIGN_PROPOSAL.md` §3-4 / §6「Step 4」
- Step 0〜3 は実装済み。`DestructionManager.Explode` はすでに **ctx(単一テーブル)方式**
- 本書は Step 4 の3本のうちの1本目。**4c(絨毯爆撃)・4d(バズーカ)には手を触れないこと**

## 1. 実装前に必ずやること

コードを1行も書く前に、以下を調べて**実装計画を報告**すること。承認を待ってから着手する。

1. `WeaponServer.lua` のリモート爆弾の設置処理と `detonateBombs`(旧L266付近)の現在の実装
2. `DestructionManager.Explode` が `ctx.scoreScale` をどの加点に掛けているか。**特に全壊ボーナス(+500点)に掛かっているかどうか**
3. `NPCManager.OnExplosion` の市民撃破加点(+100点)が `ctx.scoreScale` を見ているか
4. `EnemyManager.OnExplosion` の敵撃破加点が `ctx.scoreScale` を見ているか
5. `UIController.client.lua` の `Hud` リモート購読部分の構造(既存の `"time"` / `"hit"` / `"threat"` の処理の書き方)

報告には「どのファイルの何行目をどう変えるか」を含めること。

## 2. 目的

| # | 内容 | 狙い |
|---|---|---|
| 4a | 設置に距離制限をかける | 「近づいて仕掛ける」武器にして、プレイヤーが移動する理由を作る |
| 4b | 同時起爆数に応じたスコア倍率 | **1入力で大量処理**できるようにして、クリック疲れを減らす |

4a だけを入れると単なる弱体化になる。**必ず両方を同じコミットに入れること。**

## 3. Config への追加(`ReplicatedStorage/Config.lua`)

`Config.Weapons.RemoteBomb` に以下を追加する。既存キー(`DisplayName` / `SlotKey` / `Radius=15` / `Cooldown=1` / `MaxBombs=10`)は変更しない。

| キー | 値 | 意味 |
|---|---|---|
| `MaxPlaceDistance` | `50` | プレイヤーからこの距離を超える設置を拒否する(**XZ平面の水平距離**で判定) |
| `ChainBonus` | 下記テーブル | 同時起爆数 → スコア倍率 |
| `ChainAffectsBuildingBonus` | `false` | 全壊ボーナス(+500点)には倍率を掛けない |
| `ChainAffectsEnemy` | `false` | 敵の撃破報酬には倍率を掛けない |

```lua
ChainBonus = {
    { min = 1, mult = 1 },
    { min = 3, mult = 2 },
    { min = 5, mult = 3 },
    { min = 8, mult = 5 },
},
```

**倍率の決め方**: 起爆した個数 `count` に対し、`min <= count` を満たす中で最大の `min` を持つエントリの `mult` を採用する。テーブルは昇順に並んでいる前提でよいが、順序に依存しない走査にすること(将来の編集で壊れないように)。

## 4. 4a: 設置の距離制限

### 4-1. 判定

- 判定は**サーバー側**で行う(`WeaponServer.lua`)。クライアントは変更しない
- プレイヤーの `HumanoidRootPart.Position` と設置要求座標の **XZ距離**が `MaxPlaceDistance` を超えたら拒否
- **Y(高さ)は判定に含めない。** 3D距離にすると、地上から高層ビルの壁面をクリックしたときに高さ分で弾かれて理不尽になる

### 4-2. 拒否時の挙動

- 爆弾を生成しない
- **設置数カウント(`MaxBombs` の消費)を増やさない**
- **クールダウンを消費しない**
- `Hud` リモートで `{ kind = "notice", text = "近づいて設置してください" }` を該当プレイヤーにだけ送る

### 4-3. UIController 側(`"notice"` の表示)

`Hud` の `"notice"` は現在未使用。以下で新規実装する。

- 画面中央よりやや下に短いテキストを表示し、**1.2秒でフェードアウト**
- **連続して届いても重ならないこと**(連打で設置を試すと毎回届くため)。表示中に次が来たらタイマーをリセットして文言を差し替えるだけにする
- 中央テロップ(段階昇格用)とは**別のラベル**にする。テロップを上書きしないこと

## 5. 4b: 連鎖ボーナス

### 5-1. サーバー側(`WeaponServer.lua` / `detonateBombs`)

1. 起爆時、起爆対象の爆弾数 `count` を数える
2. `ChainBonus` から倍率 `mult` を決める
3. 各爆発の `Explode(ctx)` に `scoreScale = mult` を渡す
4. `Hud` リモートで `{ kind = "chain", mult = mult, count = count }` を送る。**`mult == 1` のときは送らない**(×1の表示は邪魔なだけ)

**物理量の制限は追加しない。** `Config.Debris.MaxRealPerExplosion`(30)と `Config.Performance.MaxUnanchoredParts`(1000)がすでに全体を頭打ちにしているため、新しい上限キーは作らないこと。もしタブレット実機で30fpsを割った場合は `MaxUnanchoredParts` を下げて対処する(Config 1行)。

### 5-2. 倍率を掛ける先・掛けない先

| 加点 | 倍率 | 備考 |
|---|---|---|
| ブロック破壊(10点) | **掛ける** | 連鎖の本体 |
| 市民NPC撃破(100点) | **掛ける** | 決定済み(`THREAT_DESIGN_PROPOSAL.md` §7-8) |
| 全壊ボーナス(500点) | **掛けない** | `ChainAffectsBuildingBonus = false`。8連鎖で2,500点になると★閾値の設計が壊れる |
| 敵の撃破報酬(警官300点 / パトカー500点) | **掛けない** | `ChainAffectsEnemy = false`。敵撃破スコアは現状すでに無制限に伸びる問題(`CURRENT_SPEC.md` §10-1)を抱えており、倍率を乗せると悪化する |
| 撃破時のタイム報酬(+3秒 / +8秒 / +10秒) | **掛けない** | 秒数に倍率を掛けるとタイム経済が壊れる。**スコアだけが対象** |

上記のうち、`NPCManager` と `EnemyManager` の加点箇所で `ctx.scoreScale` を参照していない場合は追加すること(それぞれ1〜2行のはず)。`ctx.scoreScale` が `nil` のときは `1` として扱う。

### 5-3. UIController 側(`"chain"` の表示)

- スコア表示の近くに `×3 CHAIN!` を大きめに表示し、**0.8秒でフェードアウト**
- 倍率に応じて色を変える(×2:白 / ×3:黄 / ×5:橙 など)。ハードコードでよい
- タブレットエミュレータで読めるサイズにすること

## 6. 受け入れ基準

**すべて Studio のプレイテストで確認する。1項目でも欠けたら完了としないこと。**

### 4a

- [ ] プレイヤーの足元付近をクリック → 従来どおり爆弾が設置される
- [ ] 50stud以上離れた地点をクリック → 設置されず「近づいて設置してください」が出る
- [ ] 拒否されたとき、画面右のカウンタ(残り爆弾数)が**減っていない**
- [ ] 拒否を10回連打しても、文言が重なって読めなくなったりしない
- [ ] **地上から高層ビルの上の方の壁をクリックしたとき、水平距離が50以内なら設置できる**(高さで弾かれない)
- [ ] 拒否されても中央テロップ(「警察が出動した!」など)が消えたり上書きされたりしない

### 4b

- [ ] 1個だけ起爆 → 倍率表示は出ない。スコアは従来どおり
- [ ] 3個同時起爆 → `×2 CHAIN!` が出て、ブロックのスコアが2倍で入る
- [ ] 5個同時起爆 → `×3 CHAIN!`
- [ ] 8個以上同時起爆 → `×5 CHAIN!`
- [ ] 連鎖で建物を全壊させたとき、**全壊ボーナスは500点のまま**(2500点にならない)
- [ ] 連鎖で建物を全壊させたとき、**タイムの増加は+10秒のまま**(倍にならない)
- [ ] 連鎖の巻き添えで市民NPCを倒すと、100点に倍率が掛かって入る
- [ ] 連鎖の巻き添えで警官を倒しても、**300点のまま**(倍率が掛からない)
- [ ] 10個を同時起爆したときに、目に見えるカクつきが出ない
- [ ] サーバー・クライアント両方の出力ウィンドウに赤いエラーが出ない

### 非破壊確認(先にこれを見ること)

- [ ] バズーカ・エアストライクが従来どおり撃てる
- [ ] 市民NPCの即死・パニック逃走・頭数維持が従来どおり
- [ ] 敵(パトカー・警官)の湧き・移動・降車・撃破が従来どおり
- [ ] ラウンドが LOBBY → BATTLE → RESULT を1周する

## 7. `SETUP.md` の更新

「ゲームバランスの調整」の節に以下を追記する。

- `Config.Weapons.RemoteBomb.MaxPlaceDistance`(既定50): この距離より遠くには爆弾を置けない。小さくするほど「近づいて仕掛ける」武器になり、プレイヤーが移動する
- `Config.Weapons.RemoteBomb.ChainBonus`: 同時に起爆した個数に応じたスコア倍率。`min` が「その倍率になる最低個数」

「遊び方(操作方法)」の表に変更はない。

## 8. やらないこと

- エアストライク・バズーカへの変更(それぞれ4c・4dの担当)
- 新しい物理量の上限キーの追加
- `Config.Debris` / `Config.Performance` の値の変更
- クライアント(`WeaponClient.client.lua`)への変更
