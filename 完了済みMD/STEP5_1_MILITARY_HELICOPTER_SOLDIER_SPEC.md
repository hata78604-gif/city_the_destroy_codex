# STEP5_1_MILITARY_HELICOPTER_SOLDIER_SPEC.md  
# Step 5-1：軍用ヘリによる兵士投入＋5連射攻撃 実装指示書

## 0. 最初に行うこと

この指示書に基づいて作業してください。

**実装する前に、変更計画を報告してください。私の承認を待ってから実装してください。**

変更計画には、最低限以下を含めてください。

1. 実際に変更するファイル
2. 現在の実装を読んで確認した関数名・データ構造
3. 軍用ヘリを敵として扱わず、演出専用にする方法
4. ヘリ到着待ち中に編成が全滅扱いにならない方法
5. 兵士の5連射と、0.5秒単位のタイム減少をまとめて処理する方法
6. ラウンド終了をまたぐ`task.delay`や接続を無効化する方法
7. 既存機能への影響範囲
8. テスト方法

作業開始前に以下を読んでください。

- `HANDOFF.md`
- `CURRENT_SPEC.md`
- `THREAT_DESIGN_PROPOSAL.md`
- `PROGRESS.md`
- `SETUP.md`
- 実際のLuaコード一式

仕様が食い違う場合は、**`CURRENT_SPEC.md`と実コードを優先**してください。

また、最初に`git status`を確認してください。未コミットの変更や、この指示書の前提と食い違うコードを発見した場合は、勝手に上書きせず変更計画の段階で報告してください。

---

# 1. 目的

★2到達時に、軍用ヘリが飛来して兵士4人を道路上へ投入する機能を追加します。

兵士は、1回の攻撃で機関銃を5連射します。

- 1発命中するごとに、共有タイムへ0.5秒の損失候補を加える
- 5発すべて命中した場合は合計−2.5秒
- 5発分のタイム減少は、連射終了時に1回だけ`RoundClock.Add`へ渡す
- 建物の陰に隠れた場合は、その時点以降の弾を回避できる
- 兵士撃破ではスコアのみ入り、タイムは増えない

軍用ヘリは戦闘する敵ではなく、**兵士投入を分かりやすく見せるための演出専用オブジェクト**です。

---

# 2. 今回の固定方針

## 2-1. 軍用ヘリ

軍用ヘリには以下を持たせません。

- HP
- 被弾判定
- 撃破スコア
- タイム報酬
- 攻撃
- 頭上の「!」マーカー
- 画面端の方向インジケータ
- `squadId`の生存数
- `workspace.Enemies`への配置

軍用ヘリはクライアント側で生成する短時間の演出モデルとします。

既存の`Effect`リモートを使用し、**新しいRemoteEventは追加しないでください。**

軍用ヘリはToolboxや外部モデルを使わず、`EffectsClient.client.lua`内でPartを組み合わせた簡易モデルとして生成してください。

## 2-2. 兵士

兵士は既存の警官と同じ軽量方式を使います。

- Humanoidを使わない
- 全パーツは通常時`Anchored=true`
- 共有Heartbeatで移動
- 撃破時のみラグドール化
- `spawnEnemy(type, position, squadId)`を必ず通す
- `workspace.Enemies`へ配置
- `Destructible`タグは付けない

既存の警官をコピーして別実装にするのではなく、`Body="human"`と既存の直進移動処理を再利用してください。

## 2-3. タイム報酬

兵士を倒してもタイムは増やしません。

```lua
TimeReward = 0
```

敵をタイムの供給源にしないという既存方針を維持します。

## 2-4. 今回は実装しないもの

以下は今回のスコープ外です。

- スナイパー
- 戦車
- 戦車砲
- 敵による建物破壊
- 怪獣
- プレイヤーの巨大化
- 軍用ヘリの攻撃
- 軍用ヘリの撃破
- `Movement="air"`の敵AI
- ヘリが旋回・追跡し続ける処理
- アイテムドロップ
- 日替わりミッション
- `Damage.Invincible`の変更
- `Damage.MaxLossPerMinute`の変更

---

# 3. 変更するファイル

| ファイル | 変更内容 |
|---|---|
| `ReplicatedStorage/Config.lua` | 兵士、軍用ヘリ演出、★2編成、サウンド設定を追加 |
| `ServerScriptService/Modules/EnemyManager.lua` | 輸送編成、投入待ち数、兵士の5連射攻撃を追加 |
| `StarterPlayer/StarterPlayerScripts/EffectsClient.client.lua` | 軍用ヘリ、投入演出、機関銃の曳光弾を追加 |
| `StarterPlayer/StarterPlayerScripts/UIController.client.lua` | 0.5秒単位の表示、リザルトの「警察」表記を「敵」へ変更 |
| `SETUP.md` | Step 5-1の動作確認手順を追加 |
| `CURRENT_SPEC.md` | 実装後の現状仕様を反映 |
| `PROGRESS.md` | 実装内容、妥協点、ハマった点、申し送りを追記 |
| `THREAT_DESIGN_PROPOSAL.md` | 旧「攻撃するヘリ」案を、演出専用輸送ヘリ＋兵士投入案へ更新 |

原則として、以下は変更しません。

- `ThreatManager.lua`
- `GameManager.server.lua`
- `RoundClock.lua`
- `DestructionManager.lua`
- `NPCManager.lua`
- `WeaponServer.lua`
- `WeaponClient.client.lua`

実コードの構造上どうしても変更が必要な場合は、実装前の変更計画で理由を報告してください。

---

# 4. Config追加・変更値

## 4-1. `Config.Threat.EnemyTypes.Soldier`

```lua
Soldier = {
	DisplayName = "兵士",
	Body = "human",
	BodyColors = {
		Shirt = Color3.fromRGB(72, 82, 55),
		Pants = Color3.fromRGB(58, 64, 45),
	},
	Headwear = "helmet",
	HelmetColor = Color3.fromRGB(64, 72, 48),

	Hits = 1,
	HitCooldown = 0.25,

	Movement = "direct",
	ApproachSpeed = 20,
	MoveSpeed = 13,
	StopDistance = 80,

	AttackType = "burst",
	AttackRange = 100,
	AttackInterval = 5.0,
	AttackStartJitter = 1.5,
	Telegraph = 0,

	BurstCount = 5,
	BurstInterval = 0.12,
	TimePenalty = 0.5,

	ScoreReward = 400,
	TimeReward = 0,

	Parts = 7,
	SpawnY = 3,
}
```

### 値の理由

| キー | 値 | 意味・理由 |
|---|---:|---|
| `Hits` | 1 | 兵士は数で圧力を出す。HPを増やしてクリック疲れを作らない |
| `ApproachSpeed` | 20 | 警官と同じ。遠距離から戦闘位置まで素早く来る |
| `MoveSpeed` | 13 | 警官と同じ。プレイヤーが走れば振り切れる余地を残す |
| `StopDistance` | 80 | 警官と同じ距離帯を基準にし、今回の差を攻撃方式へ絞る |
| `AttackRange` | 100 | バズーカ射程140より短く、一方的に撃たれる距離を作らない |
| `AttackInterval` | 5.0 | 1回最大−2.5秒と強いため、連射後の休止時間を長くする |
| `AttackStartJitter` | 1.5 | 4人が同じ瞬間に一斉射撃するのを防ぐ |
| `BurstCount` | 5 | ユーザー決定 |
| `BurstInterval` | 0.12 | 5発を約0.48秒で撃ち切り、機関銃らしさを出す |
| `TimePenalty` | 0.5 | 1発あたりの損失。全弾命中で−2.5秒 |
| `ScoreReward` | 400 | 警官300点より少し高く、危険度の差をスコアに反映 |
| `TimeReward` | 0 | 敵をタイム供給源にしない |
| `Hits` | 1 | 兵士の強さは攻撃で表現し、耐久力では表現しない |

`AttackInterval`は**連射開始から次の連射開始まで**の間隔として扱い、同じ兵士の連射が重複しないようにしてください。

## 4-2. `Config.Threat.Transports`

`EnemyTypes`とは別に、演出専用輸送機の設定を追加してください。

```lua
Transports = {
	MilitaryHelicopter = {
		DisplayName = "軍用ヘリ",

		Altitude = 45,
		FlightSpeed = 90,
		OutsideMargin = 80,

		DeploySpread = 6,
		DeployInterval = 0.15,

		RotorSpeed = 6,
		Parts = 9,
	},
}
```

### 値の理由

| キー | 値 | 意味・理由 |
|---|---:|---|
| `Altitude` | 45 | 道路上を通過する前提で、地上から十分見える高さ |
| `FlightSpeed` | 90 | 遅すぎて待たされず、速すぎて見逃さない速度 |
| `OutsideMargin` | 80 | 街の外側から現れて、街の外側へ飛び去るための余白 |
| `DeploySpread` | 6 | 道路幅16の内側へ4人を分散でき、建物へめり込みにくい |
| `DeployInterval` | 0.15 | 4人を同一フレームに生成せず、負荷と見た目を少し分散 |
| `RotorSpeed` | 6 | 1秒あたりのローター回転数の初期値 |
| `Parts` | 9 | パーツ予算の記録用。コードからは参照しなくてもよい |

数値は必ずConfigから読み、コードへ重複して直書きしないでください。

## 4-3. ★2 Stage

`Config.Threat.Stages`へ2件目を追加します。

```lua
{
	Name = "★2 軍隊",
	Threshold = 4000,
	Telop = "軍の増援が到着した!",
	Sound = "MilitaryAlert",
	RespawnDelay = 20,

	Squad = {
		{
			transport = "MilitaryHelicopter",
			cargo = {
				{ type = "Soldier", count = 4 },
			},
		},
	},
},
```

### 閾値について

`Threshold = 4000`は暫定値です。

元の★2案の開始値を維持しつつ、★1の1000点から明確な間隔を空けるための初期値です。

Step 4でスコアの伸び方が変化しているため、Step 5-1のテスト後に以下を記録し、必要ならConfigだけで調整します。

- ★1到達時刻
- ★2到達時刻
- ★2到達時の街の破壊率
- 警官撃破スコアが★2到達へ占める割合
- 通常プレイで★2まで到達できるか

今回、★1の閾値は変更しないでください。

## 4-4. サウンド

`Config.Sounds`へ以下を追加してください。

```lua
MilitaryAlert = "",
MilitaryHelicopter = "",
SoldierBurst = "",
```

空文字の場合は鳴らさず、エラーにしない既存作法を維持してください。

---

# 5. ★2編成データの扱い

## 5-1. 通常敵と輸送編成を分ける

現在の`DeploySquad`は、通常の以下の形式を処理しています。

```lua
{ type = "PoliceOfficer", count = 2 }
```

これに加えて、以下の形式を処理できるようにしてください。

```lua
{
	transport = "MilitaryHelicopter",
	cargo = {
		{ type = "Soldier", count = 4 },
	},
}
```

処理の考え方は以下です。

```lua
if entry.type then
	-- 既存の通常敵スポーン
elseif entry.transport then
	-- 新しい輸送投入処理
else
	-- warnして安全にスキップ
end
```

既存の通常敵スポーン処理を壊したり、大きく書き換えたりしないでください。

## 5-2. データ検証

輸送処理開始前に以下を検証してください。

- `Config.Threat.Transports[entry.transport]`が存在する
- `entry.cargo`がテーブルである
- 各cargoに`type`と正の`count`がある
- `Config.Threat.EnemyTypes[cargo.type]`が存在する

不正データを発見した場合は、サーバーを停止させず`warn`して該当項目だけスキップしてください。

---

# 6. 投入待ち中の編成全滅判定

## 6-1. 必須対策

軍用ヘリが飛来している数秒間は、まだ兵士が地上に存在しません。

そのままでは`CountAlive(squadId) == 0`となり、ThreatManagerが「編成全滅」と誤判定して次の部隊を予約する可能性があります。

この対策として、EnemyManager内に編成ごとの投入待ち数を追加してください。

例：

```lua
local pendingBySquad = {}
```

## 6-2. CountAliveの契約

`CountAlive(squadId)`は以下の合計を返すようにします。

```text
現在生存している敵の数
＋
その編成で投入待ちになっている敵の数
```

軍用ヘリそのものは数えません。

## 6-3. pendingの増減

1. ヘリ演出を開始する前に、cargoの合計人数を`pendingBySquad[squadId]`へ加算
2. 各兵士を生成する直前または生成成功直後に1減らす
3. 生成失敗時も必ず1減らして`warn`
4. `Clear()`時に全pendingを消す
5. ラウンドをまたいだ遅延処理が無効になった場合も、古いpendingを残さない

pendingが負数にならないように防御してください。

---

# 7. 軍用ヘリの飛行経路

## 7-1. 投入地点

既存の道路交点候補と、既存の`MinDistanceFromPlayer`判定を再利用してください。

投入地点は以下を満たします。

- 道路交点
- プレイヤーから原則100stud以上
- 既存の道路座標を使用
- 座標をConfigへコピーしない
- 候補が無い場合は既存の安全なフォールバックを使用

## 7-2. 飛行方向

投入地点を通る道路のX方向またはZ方向をランダムに1つ選びます。

例：

- X方向なら、街のX最小値より外側から飛来し、X最大値より外側へ退出
- Z方向なら、街のZ最小値より外側から飛来し、Z最大値より外側へ退出

開始地点と終了地点は、道路線の最小・最大座標へ`OutsideMargin`を加えて求めてください。

軍用ヘリは道路上空を直線飛行します。

今回、旋回・追跡・周回飛行は実装しません。

## 7-3. 時刻の導出

以下を距離と速度から算出してください。

```text
投入地点までの時間
全飛行時間
```

固定の待ち時間を別管理して、ヘリの見た目と兵士の出現時刻がずれないようにしてください。

サーバーからクライアントへ、少なくとも以下を送ります。

```lua
{
	startPosition = ...,
	dropPosition = ...,
	endPosition = ...,
	speed = ...,
	timeToDrop = ...,
	totalDuration = ...,
	count = 4,
	spread = ...,
}
```

---

# 8. 軍用ヘリ演出

## 8-1. Effect種別

既存の`Effect`リモートへ、新しい種別を追加してください。

```text
"militaryHelicopter"
```

必要なら投入瞬間用に以下も追加して構いません。

```text
"militaryDeploy"
```

新しいRemoteEventは作りません。

## 8-2. ヘリモデル

`EffectsClient.client.lua`で簡易モデルを生成します。

推奨構成：

- 胴体
- コックピット
- テール
- メインローター
- テールローター
- 左右のスキッド
- 左右の小さな側面パーツ

すべて以下にしてください。

```lua
Anchored = true
CanCollide = false
CanTouch = false
CanQuery = false
```

Humanoid、Motor6D、Toolboxモデルは使用しません。

## 8-3. アニメーション

- `Model:PivotTo()`で移動
- メインローターとテールローターを回転
- 飛行速度はConfig値を使用
- 投入地点通過時に、短時間のロープまたは降下線を4本表示
- 地面に軽い粉塵またはリングを1個表示
- 画面を大きく揺らさない
- 大音量の爆発音を使わない

## 8-4. 後始末

飛行終了後は以下を必ず破棄してください。

- ヘリモデル
- ローター更新接続
- ロープ
- 粉塵
- 一時的なSound
- TweenまたはRenderStepped接続

途中でエラーが発生しても、可能な限り後始末される構造にしてください。

---

# 9. 兵士の生成

ヘリが投入地点へ到達した時刻に、兵士4人を生成します。

- 全員同じ`squadId`を引き継ぐ
- 必ず`spawnEnemy("Soldier", position, squadId)`を通す
- `DeploySpread`の範囲内で分散
- `DeployInterval`秒ずつ生成をずらす
- 道路幅の外へ出すぎない
- 同一座標へ完全に重ねない
- 生成時は既存の`enemySpawn`演出を使用してよい

軍用ヘリは`squadId`を持つ敵として登録しないでください。

---

# 10. ラウンド境界と遅延処理

## 10-1. トークン

軍用ヘリ到着待ちと兵士の生成には`task.delay`が必要になります。

ラウンド終了後に兵士が出現しないよう、EnemyManager側で世代トークンを使用してください。

例：

```lua
local deployToken = 0
```

- `Clear()`時に加算
- 遅延処理作成時に現在値を保存
- 実行時に値が違えば何もしない
- `active=false`または攻撃停止状態でも生成しない

既存に同等のトークンがある場合は、新しく重複して作らず再利用してください。

## 10-2. 必須挙動

- BATTLE終了後に新しい兵士が湧かない
- RESULT中に兵士が攻撃しない
- 次のLOBBYで古い兵士が残らない
- 次ラウンドへ古いpendingが持ち越されない
- ラウンドを繰り返しても遅延処理が増殖しない

---

# 11. 兵士の5連射攻撃

## 11-1. AttackType

新しい攻撃種別を追加します。

```lua
AttackType = "burst"
```

既存の`"shoot"`処理は変更しすぎず、攻撃種別に応じた小さな分岐にしてください。

例：

```lua
if etype.AttackType == "shoot" then
	-- 既存
elseif etype.AttackType == "burst" then
	-- 新規
end
```

## 11-2. 標的

- 連射開始時に標的プレイヤーを1人決定
- 5発の途中で別プレイヤーへ標的を変更しない
- 標的が退出、死亡、Character消失した場合は残りを中止
- 兵士が撃破された場合は、その後の未発射弾を中止

## 11-3. 各弾の判定

5発それぞれの発射時点で、以下を再判定してください。

- 標的が有効か
- 攻撃距離内か
- `RangeGrace`を含めた距離条件
- 建物による遮蔽
- 兵士が生存しているか
- BATTLE中か

判定には既存の警官攻撃で使用している距離・遮蔽ロジックを再利用してください。

プレイヤーの位置は連射開始時に固定せず、**各弾発射時点の現在位置**を使用します。

これにより、1～2発目を受けたあと建物へ隠れれば、残りの弾を回避できます。

## 11-4. 曳光弾

各弾ごとに新しいEffect種別を送ります。

```text
"enemyBurstTracer"
```

見た目：

- 黄色またはオレンジの細い線
- 表示時間は約0.06～0.1秒
- 発射元から、その弾が判定した地点まで表示
- 当たり・外れの結果に関係なく表示
- 赤い予告線とは別物だと分かる色にする

機関銃音は各弾で5個鳴らさず、**連射開始時に1回だけ**`SoldierBurst`を再生してください。

## 11-5. タイム減少の集約

各弾が命中するたびに`hitCount`を増やします。

5発の処理終了後に以下を1回だけ実行します。

```lua
local loss = hitCount * etype.TimePenalty
deps.addTime(-loss, "soldierBurst", targetPlayer)
```

例：

| 命中数 | タイム減少 |
|---:|---:|
| 0 | 0秒 |
| 1 | −0.5秒 |
| 2 | −1.0秒 |
| 3 | −1.5秒 |
| 4 | −2.0秒 |
| 5 | −2.5秒 |

`hitCount == 0`の場合は`addTime`を呼ばないでください。

連射途中で兵士が倒されても、それ以前に命中済みの弾は連射終了予定時刻に集約して反映して構いません。

ただし、ラウンドが終了していた場合は反映しないでください。

## 11-6. 同期防止

複数兵士の連射開始時刻が揃わないようにします。

- 生成時に`AttackStartJitter`の範囲で初回攻撃時刻をずらす
- 同一兵士の連射中は次の連射を開始しない
- 既存のスポーン間隔だけに依存せず、明示的にずらす

---

# 12. 0.5秒単位のUI表示

現在のタイム増減表示は整数への丸め処理を持っています。

今回、−0.5秒、−1.5秒、−2.5秒を表示できるように変更してください。

## 12-1. 表示形式

- 整数なら小数点を出さない
- 0.5秒単位なら小数1桁を出す

例：

```text
+10秒!
-1秒!
-1.5秒!
-2.5秒!
```

## 12-2. 浮動小数誤差

`1.499999999`のような値をそのまま表示しないでください。

表示前に0.1秒単位へ丸めたうえで、整数かどうかを判定してください。

既存のタイム加算、警官被弾、建物全壊表示を壊さないでください。

タイマー本体の表示は、現状どおり分・秒の整数表示で構いません。変更するのは主にフローティング表示です。

---

# 13. リザルトの撃破数表記

現在、`EnemyManager.GetKillCounts()`は敵の種類を問わない合計ですが、リザルト表示は`(警察 N)`になっています。

兵士追加後は表記が不正確になるため、以下へ変更してください。

```text
(敵 N)
```

または、既存UIに自然に収まるなら、

```text
敵撃破 N
```

でも構いません。

今回は敵種別ごとの個別集計は追加しません。

`Result`リモートのデータ構造も変更しません。

---

# 14. 既存動作を壊さないための注意

## 14-1. 変更禁止または最小限

- 警官の攻撃間隔・射程・ダメージを変えない
- パトカーの道路AIを変えない
- パトカーの無制限降車を変えない
- バズーカ射程140を変えない
- 武器のクールダウンを変えない
- エアストライクの投下数を変えない
- リモート爆弾の倍率を変えない
- `Damage.Invincible = 0`を変えない
- `Damage.MaxLossPerMinute = 0`を変えない
- 建物全壊の+10秒を変えない
- RoundClockのdeadline方式を変えない
- `ThreatManager`に★2専用の`if`分岐を追加しない

## 14-2. UI

新しいGuiObjectを追加する場合は、クリック入力を吸わないよう必ず以下を確認してください。

```lua
Active = false
```

今回の曳光弾とヘリは3D演出なので、原則として新しいHUDラベルは不要です。

## 14-3. 敵の識別

兵士は通常の敵なので、以下を持ちます。

- 頭上の「!」
- 画面端の▲
- バズーカの直撃判定
- 撃破数
- スコア報酬

軍用ヘリはこれらを一切持ちません。

## 14-4. パフォーマンス

- ヘリモデルをサーバーへ複製しない
- ヘリに物理演算を使わない
- ローター回転のために複数のHeartbeat接続を作らない
- 1機につき更新接続は最大1本
- 飛行終了後に確実に切断
- 曳光弾は短時間でDestroy
- 曳光弾をDebrisキューへ入れない
- 兵士は既存の共有Heartbeatへ追加

---

# 15. ドキュメント更新

## 15-1. `CURRENT_SPEC.md`

実装後の実コードを読み、以下を反映してください。

- `EnemyManager`の輸送編成処理
- `pendingBySquad`を含む`CountAlive`の契約
- `Soldier`の全Config値
- `Config.Threat.Transports.MilitaryHelicopter`
- ★2のStage
- 兵士の5連射処理
- タイム損失を連射終了時に集約する理由
- 軍用ヘリが敵ではないこと
- 新しいEffect種別
- リザルト表記の変更
- スナイパーと戦車が未実装であること
- ★2閾値4000が暫定であること

章番号は指示書の想定値をそのまま使わず、実際の文書の続きに合わせてください。

## 15-2. `PROGRESS.md`

以下を追記してください。

- 実装したもの
- 軍用ヘリをクライアント演出にした理由
- pendingを導入した理由
- 5連射のダメージを1回へ集約した理由
- 暫定値
- ハマった点
- 次のStep 5-2への申し送り
- ユーザーがStudioで確認すること

## 15-3. `THREAT_DESIGN_PROPOSAL.md`

旧案の以下を現仕様に合わせて更新してください。

旧案：

- `Helicopter`をEnemyTypesへ登録
- `Movement="air"`
- ヘリが攻撃する
- ヘリにHPと撃破報酬がある

新案：

- `MilitaryHelicopter`は`Transports`内の演出専用
- ヘリは攻撃しない
- ヘリは撃破できない
- ヘリは編成生存数に含まれない
- ★2では兵士4人を投入
- 兵士が5連射する
- スナイパーはStep 5-2
- 戦車は後続Step

過去の設計経緯を消すのではなく、「Step 5着手時に変更した」と分かる形で更新してください。

## 15-4. `SETUP.md`

既存のPhase番号を確認し、次の空いている番号でStep 5-1の確認項目を追加してください。

---

# 16. 受け入れ基準

## 16-1. 既存機能

- [ ] バズーカをクリック・長押しで撃てる
- [ ] バズーカの射程140が維持されている
- [ ] エアストライクが18発の絨毯爆撃として動く
- [ ] リモート爆弾の設置・起爆・連鎖倍率が動く
- [ ] 市民NPCの徘徊・撃破・パニック逃走が動く
- [ ] 建物全壊で+500点と+10秒が入る
- [ ] ★1でパトカー2台と警官が従来どおり出る
- [ ] パトカーが道路を走り、警官を降ろす
- [ ] RESULT中に武器と敵の攻撃が止まる
- [ ] 出力ウィンドウに赤いエラーがない

## 16-2. ★2到達

- [ ] 4000点到達で★表示が`★★`になる
- [ ] 中央に「軍の増援が到着した!」と表示される
- [ ] ★2到達ログに開始からの秒数とスコアが出る
- [ ] ★2到達時に軍用ヘリが1機だけ飛来する
- [ ] ヘリ到着待ち中に「編成全滅」と誤判定されない
- [ ] ヘリ到着前に次のヘリが重複して来ない

## 16-3. 軍用ヘリ

- [ ] 街の外側から現れる
- [ ] 道路上空を直線飛行する
- [ ] 投入地点の上を通過する
- [ ] 兵士投入後、反対側の街の外へ飛び去る
- [ ] メインローターが回転している
- [ ] テールローターが回転している
- [ ] ヘリに「!」が付かない
- [ ] ヘリに画面端の▲が付かない
- [ ] ヘリへバズーカを撃っても撃破対象にならない
- [ ] ヘリ撃破スコアが存在しない
- [ ] 飛行終了後にモデルが残らない
- [ ] 2回目以降のwaveでも古いヘリが残らない

## 16-4. 兵士投入

- [ ] 投入地点がプレイヤーから原則100stud以上離れている
- [ ] 兵士4人がヘリ通過時に出現する
- [ ] 4人が完全に同じ座標へ重ならない
- [ ] 4人が同じ`squadId`を持つ
- [ ] 兵士投入前はpendingにより編成が生存扱いになる
- [ ] 兵士投入後にpendingが0へ戻る
- [ ] Outputにpendingの負数や不整合警告が出ない

## 16-5. 兵士の見た目・撃破

- [ ] 警官とは異なる緑系の服になっている
- [ ] ヘルメットが表示される
- [ ] 頭上に「!」が付く
- [ ] 画面外では▲が表示される
- [ ] バズーカ1発で倒せる
- [ ] 撃破時に+400点入る
- [ ] 撃破してもタイムは増えない
- [ ] 死体が既存の敵と同様に消える
- [ ] 死体がバズーカの弾を遮らない

## 16-6. 5連射

- [ ] 1回の攻撃で曳光弾が5本出る
- [ ] 約0.12秒間隔で発射される
- [ ] 曳光弾は赤ではなく黄色またはオレンジ
- [ ] 機関銃音は1連射につき1回だけ
- [ ] 5発の途中で建物へ隠れると残りが外れる
- [ ] 5発すべて命中すると−2.5秒
- [ ] 3発命中なら−1.5秒
- [ ] 1発命中なら−0.5秒
- [ ] 全弾外れならタイムが減らない
- [ ] タイム減少のUIは1連射につき1回だけ出る
- [ ] `-1.5秒!`のように小数1桁で表示される
- [ ] `-1.0秒!`ではなく`-1秒!`と表示される
- [ ] 兵士4人の連射開始時刻が完全には揃わない
- [ ] 同じ兵士の連射が重複しない

## 16-7. wave

- [ ] 兵士4人を全滅させるまで次waveは来ない
- [ ] 全滅から20秒後に次の軍用ヘリが1機だけ来る
- [ ] 20秒の間に複数の派遣予約が作られない
- [ ] ヘリは生存数へ含まれていない
- [ ] 次waveの兵士も正常に5連射する

## 16-8. ラウンド境界

- [ ] ヘリ飛来中にBATTLEが終了しても、RESULT中に兵士が湧かない
- [ ] RESULT中に兵士が射撃しない
- [ ] 次のLOBBYで兵士が全員消える
- [ ] pendingが次ラウンドへ持ち越されない
- [ ] ラウンドを3周してもヘリや兵士が多重生成されない
- [ ] 接続や遅延コールバックがラウンドごとに増殖しない

## 16-9. リザルト・実機

- [ ] リザルトの撃破数表記が`警察`ではなく`敵`になっている
- [ ] 警官と兵士の合計撃破数が正しい
- [ ] PC操作で問題がない
- [ ] モバイルエミュレータで問題がない
- [ ] 対象タブレット実機で30fpsを維持する
- [ ] ヘリ飛来、兵士4人、爆発が重なっても大きくカクつかない
- [ ] サーバー・クライアント両方のOutputに赤いエラーがない

---

# 17. テスト時の閾値短縮

通常値は以下です。

```lua
★1 Threshold = 1000
★2 Threshold = 4000
```

Studioで動作確認を繰り返す際だけ、一時的に以下へ変更して構いません。

```lua
★1 Threshold = 100
★2 Threshold = 200
```

ただし、確認後は必ず以下へ戻してください。

```lua
★1 Threshold = 1000
★2 Threshold = 4000
```

一時テスト値をコミットしないでください。

Config変更後はStudioのテストを停止し、再度プレイを開始して反映してください。

---

# 18. 実装完了時の報告

実装完了後、以下を報告してください。

1. 変更したファイル一覧
2. 各ファイルの変更内容
3. 軍用ヘリの実装方式
4. `pendingBySquad`の実装方式
5. 5連射の命中集約方式
6. ラウンド境界のキャンセル方式
7. 既存コードから変更した関数名
8. 新しく追加したConfigキー
9. 新しく追加したEffect種別
10. 実行した静的確認・テスト
11. 未確認事項
12. ユーザーがRoblox Studioで行う具体的な確認手順
13. `CURRENT_SPEC.md`、`PROGRESS.md`、`SETUP.md`、`THREAT_DESIGN_PROPOSAL.md`の更新内容

実装とドキュメント更新は**Claude CodeのSonnet**を使用してください。