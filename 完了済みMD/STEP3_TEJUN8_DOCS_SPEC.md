# STEP3 手順8 — ドキュメント更新(Step 3 の締め)

作成日: 2026-08-01
前提: 手順1〜7 実装済み・実機確認済み

**コードの変更は §1 の1か所だけ。それ以外はすべてドキュメント作業。**

---

## 0. この手順の位置づけ

Step 3(パトカー実装)の最終手順。**次に着手するのは Step 4(武器改修)。**

したがってこの手順の目的は「仕様書を最新にする」ことだけではない。
**Step 4 に入る人が迷わない状態を作ること**が本題である。具体的には:

1. いまの数値を記録する(§2)
2. **なぜその数値にしたかを記録する**(§3)← これが無いと後で戻される
3. **Step 4 で測り直しになる項目を明示する**(§4)
4. Step 3 で見つかった未解決の課題を残す(§5)

---

## 1. 唯一のコード変更: `PoliceCar.Hits` を 3 に

`Config.lua` の `Config.Threat.EnemyTypes.PoliceCar.Hits` を **5 → 3** に変更する。

変更後、Studio で1ラウンド遊んで以下を確認する。

- [ ] パトカーがバズーカ3発で破壊される
- [ ] 1発目・2発目で赤いフラッシュが出て、3発目で撃破演出になる
- [ ] 「手応えが軽すぎる」と感じないか(感じたら報告すること。4 に戻す判断もありうる)

**他の数値は一切変更しない。**

---

## 2. `CURRENT_SPEC.md` の更新

### 2-1. `Config.Threat.EnemyTypes` の表を実際の値に合わせる

`PoliceOfficer` の以下3つが変更されている。表を書き換えること。

| キー | 旧 | 新 |
|---|---|---|
| StopDistance | 45 | **80** |
| AttackRange | 60 | **100** |
| TimeReward | 3 | **0** |

`PoliceCar` の項を新設する(現状「未実装」と書かれている)。**実際の `Config.lua` を読んで転記すること。**
本書の数値と食い違った場合は `Config.lua` が正。

主要な値: `Hits = 3` / `HitCooldown = 0.25` / `Movement = "road"` / `MoveSpeed = 26` /
`StopDistance = 15` / `AttackType = "none"` / `TimePenalty = 0` / `ScoreReward = 500` / `TimeReward = 8` /
`DeployOnArrive = true` / `DeployType = "PoliceOfficer"` / `DeployCount = 2` /
`DeployInterval = 10` / `MaxDeployTrips = nil` / `DeployFallbackTime = 10` /
`DeploySideOffset = 4.5` / `DeployLongOffset = 2` / `DeployRadius`(降車エフェクト用に降格)

### 2-2. `Config.Threat.Spawn` に `Jitter = 6` を追加

### 2-3. `Stages[1]` の記述を更新

「★1警察・Threshold=1000・Squad={PoliceOfficer×4}」となっているが、実際は
**`{PoliceCar×2, PoliceOfficer×2}`**。`RespawnDelay = 20` も明記する。

### 2-4. `EnemyManager` のモジュール説明に追記

現在の説明には Step 3 で追加した機能が反映されていない。以下を追記する。

- **道路走行**(`Movement == "road"`): 道路網の交点を経由してプレイヤーに接近する
- **降車**: `DeployOnArrive` の敵は到着時に別種の敵を生成する。生成は `spawnEnemy` を通り、
  **親の `squadId` をそのまま引き継ぐ**(全滅判定を壊さないため)
- **湧き位置の分散**: 同一 `DeploySquad` 内で `Movement == "road"` の個体は交差点を重複させない。
  それ以外の個体は `Spawn.Jitter` の範囲でランダムにずれる

### 2-5. エフェクト一覧に2件追加

| 種別 | 発火 | 内容 |
|---|---|---|
| `enemyDeploy` | サーバー(`deployFromCar`) | 車の位置から青いリングが半径12まで広がって消える(0.4秒) |
| 被弾フラッシュ | サーバー(`EnemyManager` 内で完結) | `Hits > 1` の敵が被弾して生き残ったとき、`Highlight` を0.12秒だけ有効化。**リモート経由ではない** |

被弾フラッシュが**リモートを使わない**点は明記すること。エフェクト種別の一覧を見て
「`enemyHit` というリモート種別があるはず」と探す人が出るため。

### 2-6. `Config.Sounds` に `EnemyDeploy`(空文字)を追加

---

## 3. 設計判断の記録 ★この手順の本題

`CURRENT_SPEC.md` に **「Step 3 の設計判断」** という節を新設し、以下を記録する。

**数値だけを書かないこと。** 数値だけの仕様書は、後から見た人(将来の自分を含む)に
「これは適当に決めた値だ」と誤解され、理由もなく変更される。
以下はすべて実機フィードバックを経て決まった値である。

### 3-1. `MaxDeployTrips = nil`(無制限)

**当初の設計値は 2 だった。無制限に変更した。**

理由: 降車回数を使い切ったパトカーは攻撃してこない。しかし編成の全滅判定
(`CountAlive(squadId) == 0`)には数えられ続けるため、**「無害な門番」**になっていた。
結果、「使い切ったパトカーをわざと1台残せば次の波が永久に来ない」という抜け道が成立していた。

無制限にすると、放置したパトカーは警官を延々と生み続ける。
**放置コストがゼロから無限になり、抜け道が塞がる。**

**この値を有限に戻す場合は、必ず上記の抜け道への対策(離脱して消滅する等)を同時に入れること。**

### 3-2. `PoliceOfficer.TimeReward = 0`(旧 3)

理由: **スコアは「進行度」、タイムは「生存」で意味が違う。** 敵がタイムを配ると、
敵が脅威ではなく「生存資源の配達人」になる。実機で「敵を待っていれば時間が増える」
状態になっていたため、タイム報酬のみを外した。

スコア(300)は残している。スコアが増えると★が上がって危険が増すため、
**スコアを配ることは「危険と引き換えの取引」として正しく機能する。**

### 3-3. `PoliceOfficer` の StopDistance 80 / AttackRange 100(旧 45 / 60)

理由: 車も警官もプレイヤーの至近距離に集まり、**遭遇の全体が1点に潰れていた**。
爆発1発で全部片付き、プレイヤーが動く理由が無かった。警官を遠くに配置して面積を持たせた。

**注意: パトカーの `StopDistance` は 15 のまま**であり、車は依然としてプレイヤーの至近まで来る。
これは §5 に未解決の課題として残している。

### 3-4. `PoliceCar.Hits = 3`(当初 2 → 実機で 5 を試行 → 3 で確定)

理由: `MaxDeployTrips = nil` との組み合わせで考える必要がある。
**車は警官の湧きを止める「スイッチ」である。** スイッチが遠い(Hits が大きい)ほど、
止めるまでの間に警官が増え続け、クリック回数が加速度的に増える。

5 は「敵が強い」ではなく「クリックが疲れる」という結果になったため 3 に下げた。
**敵の HP を上げることは、プレイヤーの腕力を通貨にして難易度を買うことに等しい。**
今後 HP でバランスを取りたくなった場合は、まずこの記述を読むこと。

### 3-5. `Spawn.Jitter = 6`(上限 8)

**大きくしないこと。** 目的は「敵が2人いると目で分かる」ことだけであり、
**同じ交差点に湧いた2人がバズーカ1発でまとめて倒せる状態は維持すべき利点**である
(クリック回数の削減につながる)。散らしすぎると手数が増えて改悪になる。

道路幅16(中心から±8)なので、8 を超えると道路外・建物内に湧く。

### 3-6. 被弾フラッシュを `Hits > 1` の敵だけに付ける理由

`Hits = 1` の敵は1発で死ぬため、フラッシュが出る状況が存在しない。
また `Highlight` には同時描画数の上限(概ね31)があり、無駄に消費しない。

**全敵に付けたくなった場合は、`Highlight` ではなくパーツの `Color` 書き換え方式に切り替えること。**

### 3-7. 降車エフェクトを地味に保つ理由

`MaxDeployTrips = nil` かつ `DeployInterval = 10` でパトカーは2台。
**平均5秒に1回**この演出が出る。カメラシェイクや大きな効果音を付けると、
爆発・全壊・タイム増減といった既存の演出が埋もれる。

### 3-8. `DeployFallbackTime = 10` は「実装済み・未検証」★正確に書くこと

「到着できないまま10秒経ったら強制的に降車する」保険。

**通常のプレイでは発火しない。** パトカーの `MoveSpeed`(26)はプレイヤー(16)より速く、
`StopDistance` も15と短いため、車は必ず到着する。実機テストでもログは観測されていない。

したがって **「動作確認済み」と書いてはいけない。「実装済み・未検証」と書くこと。**
この保険の本来の出番は「車が瓦礫や地形に引っかかって到着できない」といった異常系であり、
狙って再現できない。

**発火した場合の読み方**(ログ: `[EnemyManager] PoliceCar 強制降車(未到着) trip=N dist=NN`):

| dist | 意味 | 対応 |
|---|---|---|
| 50以下 | 実害なし | 放置してよい |
| 150以上 | 車がプレイヤーに追いつけていない | `MoveSpeed` を見直す |

---

## 4. Step 4 で測り直しが必要な項目 ★新設する節

`CURRENT_SPEC.md` に **「Step 4 前に無効化されている測定値」** という節を作り、以下を列挙する。

Step 4(武器改修)は絨毯爆撃・リモート爆弾の連鎖・バズーカの射程を変更するため、
**Step 3 時点のバランス測定はすべて無効になる。** Step 4 完了前にこれらを調整しないこと。

| # | 項目 | 現状 | Step 4 後に何をするか |
|---|---|---|---|
| 1 | `Stages[].Threshold`(★の閾値) | ★1=1000 のみ登録 | 到達時刻を再測定。**現在、警官が無制限に湧くため撃破スコアが無限**で、★の上昇が「街の破壊」ではなく「警官狩り」で決まる状態になっている。設計意図(破壊が進行度の主役)と逆転している |
| 2 | タイム収支 | 建物を壊しながら警官を捌いてトントン | 武器の出力が変わると全部ずれる。1ラウンドの平均終了時刻を再測定 |
| 3 | **1ラウンドあたりのクリック回数** | 「腕が疲れる」水準 | Step 4 の絨毯爆撃・連鎖は「1入力で大量処理」の武器であり、**クリック疲れの本命の対策**。改善したか必ず確認する |
| 4 | `Damage.Invincible` / `MaxLossPerMinute` | どちらも 0(無効) | ★3(Step 6)で再検討。★1では不要と判断済み |
| 5 | `Config.Round.BattleTime = 120` | 未再判断 | `THREAT_DESIGN_PROPOSAL.md` §5-9 の基準(平均終了時刻150秒未満なら据え置き等)で判断 |

---

## 5. 未解決の課題 ★新設する節

`CURRENT_SPEC.md` に **「Step 3 で判明した未解決の課題」** という節を作り、以下を記録する。
**どれも Step 3 では対応しないと決めたものであり、忘れると同じ議論を繰り返すことになる。**

### 5-1. プレイヤーが移動しない(最優先)

現状、プレイヤーはほぼ立ったまま1ラウンドを終えられる。原因は2つ。

1. **バズーカに射程制限が無い。** 街は496stud四方で、中央に立てば全域に届きうる
2. **パトカーの `StopDistance = 15`。** 標的の方からプレイヤーの至近まで来るため、
   車も警官もまとめて1発で処理でき、動く理由が生まれない

**Step 4 の最優先項目。** バズーカに射程を入れるのが最も安価な対策(数値1個)。
ただし警官の `AttackRange = 100` より短くすると「一方的に撃たれる」になるため、対で調整すること。

### 5-2. アイテムドロップ構想(未着手)

建物破壊時にアイテムを落とし、移動を促す案。**Step 4 完了までは設計しないこと**
(ドロップの中身は武器と直結するため、武器が変わる前に決めると作り直しになる)。

**落とすものに「時間」を選んではいけない。** §3-2 で警官のタイム報酬を外して
「敵は時間の供給源ではない」と整理した直後に、供給源が瓦礫に移るだけになる。

有力案は**武器の出力**(絨毯爆撃1回分など)。拾いに行くので移動が生まれ、
拾えば1タップで大量処理できるのでクリック疲れも同時に緩和される。

### 5-3. 構造崩落(判断保留)

引き継ぎ済みの課題。B+C案(閾値ベースの崩落 + 崩落分の減点)が推奨だが、
A案(微小崩落)を実機で試して、浮いた構造物の見た目がどれだけ気になるかを先に判断する。

### 5-4. `Movement == "direct"` 以外の敵種別が未実装

`Helicopter` / `Soldier` / `Tank` は `Config` に定義があるが未実装。
**`Stages` に登録するとエラーになる。** ★2以降を有効化する際に実装が必要。

---

## 6. `SETUP.md` の更新

既存の Phase 構成の末尾に **Phase 7: パトカー(Step 3)** を追加する。
既存 Phase の書式(チェックボックス形式)に合わせること。

- [ ] スコア1,000点で「警察が出動した!」テロップ + ★インジケータが ★☆☆ に変わる
- [ ] パトカー2台と警官2人が湧く。**パトカー2台は必ず別々の交差点**から湧く
- [ ] パトカーが道路の上だけを走り、角で曲がりながらプレイヤーに近づいてくる
- [ ] 到着すると車の**左右のドア横**に警官2人が降り、青いリングが広がって消える
- [ ] 放置すると10秒ごとに警官が追加で降りてくる(**上限なし**)
- [ ] パトカーにバズーカを当てると赤く一瞬光る。3発で破壊できる
- [ ] **降ろされた警官を全員倒しても、パトカーが生きている限り次の波が来ない**
- [ ] パトカーも倒すと20秒後に次の波が来る
- [ ] 警官は遠く(80stud)で止まり、100studから撃ってくる
- [ ] 警官を倒してもタイムは増えない(スコア300のみ)
- [ ] ラウンド終了で敵が全部消え、次のラウンドで★が☆に戻る

---

## 7. 変更ファイル一覧

| ファイル | 内容 |
|---|---|
| `Config.lua` | `PoliceCar.Hits` を 5 → 3(**これが唯一のコード変更**) |
| `CURRENT_SPEC.md` | §2 の反映、§3〜§5 の3節を新設 |
| `SETUP.md` | Phase 7 を追加 |
| 上記以外すべて | **変更なし** |

---

## 8. 受け入れ基準

- [ ] `PoliceCar.Hits = 3` で1ラウンド遊び、破壊に3発かかることを確認した
- [ ] `CURRENT_SPEC.md` の数値が **`Config.lua` の実際の値と一致している**
      (本書の数値ではなく `Config.lua` を読んで転記すること)
- [ ] `CURRENT_SPEC.md` に §3(設計判断)§4(測り直し項目)§5(未解決の課題)の3節がある
- [ ] `DeployFallbackTime` が「実装済み・**未検証**」と書かれている(「動作確認済み」ではない)
- [ ] `SETUP.md` に Phase 7 があり、既存 Phase と書式が揃っている
- [ ] `Stages[1]` の記述が `{PoliceCar×2, PoliceOfficer×2}` になっている
- [ ] ドキュメント以外のコードに変更が入っていない(`Config.lua` の1行を除く)

---

## 9. 進め方

1. §1 の `Hits` 変更 → 実機で1ラウンド確認 → 報告
2. **`Config.lua` を実際に読んで** §2 の転記を行う
3. §3〜§6 の記述を追加
4. §8 のチェックリストを確認して報告

**§3 が本手順の中心である。** 数値の転記だけで終わらせないこと。
